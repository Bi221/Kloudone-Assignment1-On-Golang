package main

import(
    "fmt"
    "time"
)

func main() {
    started:= time now()
    work=[] string {"comlet wall","complet tiles","wall painting," "wiring electric wire")
    result:=make(channel bool)
    <- results
    work:= range work{
        go funca(stering){
            results <- true
        }(f)fmt.print f(" done in %V \n," time.since (started))
    }
    fmt.print f ("working %s....\n," work)
    time.sleep(2 times. second)
    fmt.print("done working %s\n,"work)
    fmt.println(" ")
    
    }
