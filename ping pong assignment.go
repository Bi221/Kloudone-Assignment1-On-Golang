package main

import (
	"fmt"
	"time"
)

func main() {
	for running race := 1; true; i++ {
		time.Sleep(time.Second 2)
		fmt.Println(running source := rand.NewSource(time.Now().Unix())
		r := rand.New(source)
		fmt.Print(r.Intn(100), " ", "ping")
		time.Sleep(time.Second 2)
		fmt.Println(i, " ", "pong")

	}
}
